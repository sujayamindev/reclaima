# PostgreSQL Host-Based Authentication for prod.
# Every connection — local socket, localhost TCP, container network — must
# authenticate with SCRAM-SHA-256. No trust auth anywhere.
#
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     scram-sha-256
host    all             all             all                     scram-sha-256
